package com.nouros.hrms.service;

import com.nouros.hrms.model.ShiftRotation;
import com.nouros.hrms.service.generic.GenericService;

/**
 * 
 * ShiftRotationService interface is a service layer interface which handles all
 * the business logic related to ShiftRotation model.
 * 
 * It extends GenericService interface which provides basic CRUD operations.
 * 
 * @author Bootnext ShiftRotation
 * 
 * @version 1.0
 * 
 * @since 2022-07-01
 */
public interface ShiftRotationService extends GenericService<Integer,ShiftRotation> {

	
}
